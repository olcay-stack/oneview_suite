{
  "name": "shop-floor-agents",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Backend proxy that connects the Shop-Floor Agent Index to the Anthropic API without exposing the API key to the browser.",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.1",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
